"use strict";
const student1 = {
    name: "Nitish",
    age: 25,
    greet: ((country) => `my name is ${name} my country is ${country}`)
};
const introduction = (student1) => {
    const { name, age } = student1;
    return `Welcome my name is ${name}, I am ${age}ysr old`;
};
